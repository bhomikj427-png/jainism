# Python bytecode cache (from graph/build_graph.py)
__pycache__/
*.pyc

# Oversized raster exports — render on demand from graph.dot / build_graph.py
graph/*.png

# Graphviz cleanup intermediate (created+deleted on each render; never commit)
graph/graph
